# The deployed origin, with no trailing slash. It is substituted into
# index.html at build time for the canonical URL, the Open Graph tags and
# the JSON-LD. Set this to the real domain before going public.
VITE_SITE_URL=https://REPLACE-WITH-YOUR-DOMAIN
